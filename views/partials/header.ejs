<!DOCTYPE html>
<html lang="en" class="bg-slate-900 text-white">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Weather App</title>

  <!-- Leaflet CSS -->
  <link rel="stylesheet" href="https://unpkg.com/leaflet/dist/leaflet.css" />
  <!-- Icons CSS -->
   <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css">



  <!-- Tailwind CSS -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen font-sans bg-slate-900 text-white">
<!-- Navbar -->
<nav class="bg-slate-800 shadow-lg text-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">
      
      <!-- Left: Logo -->
      <div class="flex-shrink-0 font-bold text-lg" style="display: inline;">
         <a href="/SephWares"><img src="../img/SephWares.jpg" alt="" style="width: 40px;height: 40px;"> </a><a href="/SephWares"><span style="color: blue;">SephWares</span></a> Digital Climate
      </div>

      <!-- Center: Search Form -->
      <div class="hidden sm:block w-full max-w-md mx-auto">
        <form action="/weather%20results" method="get" class="flex">
          <input
            type="text"
            name="search"
            placeholder="Enter a city..."
            class="w-full px-3 py-2 rounded-l-md focus:outline-none focus:ring-2 focus:ring-blue-400 text-black"
            required
          />
          <button
            type="submit"
            class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-r-md"
          >
            Search
          </button>
        </form>
      </div>

      <!-- Right: Navigation Links -->
      <div class="hidden sm:flex items-center space-x-6 font-bold text-lg">
        <a href="/" class="hover:text-blue-400 transition">Home</a>
        <a href="/about" class="hover:text-blue-400 transition">About</a>
      </div>
    </div>
  </div>

  <!-- Responsive (Small Screens): Search and Links -->
  <div class="sm:hidden px-4 py-3 space-y-2">
    <!-- Search -->
    <form action="/weather%20results" method="get" class="flex">
      <input
        type="text"
        name="search"
        placeholder="Enter a city..."
        class="w-full px-3 py-2 rounded-l-md focus:outline-none focus:ring-2 focus:ring-blue-400 text-black"
        required
      />
      <button
        type="submit"
        class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-r-md"
      >
        Search
      </button>
    </form>

    <!-- Links -->
    <div class="flex justify-end space-x-6 font-bold text-lg pt-2">
      <a href="/" class="hover:text-blue-400 transition">Home</a>
      <a href="/about" class="hover:text-blue-400 transition">About</a>
    </div>
  </div>
</nav>


  <!-- Leaflet JS -->
  <script src="https://unpkg.com/leaflet/dist/leaflet.js"></script>
</body>
</html>
